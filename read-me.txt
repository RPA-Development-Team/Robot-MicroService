package json format:
{
    packagename,
    date,
    time,
    path,
    repeat,
    machine_name
}

machine json format
{
    name,
    type,
    will be assigned ---> socketId
}

//machine connects to serevr via socket

//send meta data

//wait for pkg req

//send json containing pkg name & pkg cloud url & job-id

//listen for logs as json per job then to be stored



